{
  "name": "policy-stamper",
  "version": "0.2.1",
  "dependencies": {
    "mkdirp": "^0.5.1",
    "stampery": "^3.4.0"
  },
  "repository": "https://github.com/trailbot/stamper-policy",
  "policy": {
    "language": "icedcoffeescript",
    "params": {
      "secret": {
        "type": "text",
        "required": true,
        "label": "Stampery API token",
        "tip": "Your Stampery API secret token. Mail us at nestor@stampery.com for requesting one."
      },
      "proofsDir": {
        "type": "text",
        "required": true,
        "label": "Proofs output directory",
        "tip": "The path of the folder in the server where you want to store the proofs."
      }
    },
    "defaultName": "Blockchain timestamp"
  }
}
